# CMakeLists.txt for Gravity Alignment functionality
cmake_minimum_required(VERSION 3.0.2)

# 如果作为独立项目编译
if(NOT DEFINED PROJECT_NAME)
    project(gravity_alignment)
    
    find_package(catkin REQUIRED COMPONENTS
        roscpp
        rospy
        std_msgs
        sensor_msgs
        nav_msgs
        geometry_msgs
        pcl_conversions
        pcl_ros
    )
    
    find_package(PCL REQUIRED)
    find_package(Eigen3 REQUIRED)
    
    catkin_package(
        INCLUDE_DIRS include
        LIBRARIES gravity_alignment
        CATKIN_DEPENDS roscpp rospy std_msgs sensor_msgs nav_msgs geometry_msgs pcl_conversions pcl_ros
        DEPENDS PCL Eigen3
    )
    
    include_directories(
        include
        ${catkin_INCLUDE_DIRS}
        ${PCL_INCLUDE_DIRS}
        ${EIGEN3_INCLUDE_DIR}
    )
endif()

# 编译设置
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O3 -Wall")

# 头文件目录
include_directories(
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/include
)

# 测试可执行文件
add_executable(test_gravity_alignment 
    test_gravity_alignment.cpp
)

target_link_libraries(test_gravity_alignment 
    ${catkin_LIBRARIES}
    ${PCL_LIBRARIES}
)

# ROS节点可执行文件
add_executable(gravity_alignment_example 
    gravity_alignment_example.cpp
)

target_link_libraries(gravity_alignment_example 
    ${catkin_LIBRARIES}
    ${PCL_LIBRARIES}
)

# 如果要集成到Fast-LIO2项目中，添加以下内容到原有的CMakeLists.txt：
#
# # 添加重力对齐功能
# include_directories(${CMAKE_CURRENT_SOURCE_DIR})
# 
# # 修改laserMapping的编译，添加重力对齐头文件
# target_include_directories(fast_lio PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
#
# # 如果需要单独编译重力对齐功能
# add_executable(gravity_alignment_node gravity_alignment_example.cpp)
# target_link_libraries(gravity_alignment_node ${catkin_LIBRARIES} ${PCL_LIBRARIES})